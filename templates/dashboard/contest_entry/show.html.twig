{% extends 'admin.html.twig' %}

{% block title %}ContestEntry{% endblock %}

{% block body %}
    <h1>ContestEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ contest_entry.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ contest_entry.contestQuestion }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ contest_entry.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ contest_entry.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contest_entry.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ contest_entry.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contest_entry_index') }}">back to list</a>

    <a href="{{ path('contest_entry_edit', {'id': contest_entry.id}) }}">edit</a>

    {{ include('dashboard/contest_entry/_delete_form.html.twig') }}
{% endblock %}
