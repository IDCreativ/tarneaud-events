{% extends 'admin.html.twig' %}

{% block title %}ContestOption{% endblock %}

{% block body %}
    <h1>ContestOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contest_option.id }}</td>
            </tr>
            <tr>
                <th>OptionText</th>
                <td>{{ contest_option.optionText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contest_option_index') }}">back to list</a>

    <a href="{{ path('contest_option_edit', {'id': contest_option.id}) }}">edit</a>

    {{ include('dashboard/contest_option/_delete_form.html.twig') }}
{% endblock %}
