<form class="d-inline" method="post" action="{{ path('general_configuration_delete', {'id': general_configuration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ general_configuration.id) }}">
    <button class="btn btn-outline-danger btn-sm"><i class="fal fa-trash-alt"></i></button>
</form>
