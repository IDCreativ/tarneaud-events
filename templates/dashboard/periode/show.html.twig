{% extends 'admin.html.twig' %}

{% block title %}{{ periode }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col mb-4">
            <h1>{{ periode }}</h1>
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th>#</th>
                        <td>{{ periode.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ periode.name }}</td>
                    </tr>
                    <tr>
                        <th>Début</th>
                        <td>{{ periode.dateStart ? periode.dateStart|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Fin</th>
                        <td>{{ periode.dateEnd ? periode.dateEnd|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
<div class="container mt-3">
    <div class="row">
        <div class="col">
            <a href="{{ path('periode_index') }}"><i class="fal fa-long-arrow-left me-1"></i>Retour</a>
        </div>
    </div>
</div> 
{% endblock %}
