<form class="d-inline" method="post" action="{{ path('event_date_delete', {'id': event_date.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir supprimer cet élément ? Cette action est irréversible !');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_date.id) }}">
    <button class="btn btn-outline-danger"><i class="fal fa-trash-alt"></i></button>
</form>
