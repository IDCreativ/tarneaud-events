<div class="card">
    <div class="card-header">
        Je m'inscris ou <a href="{{ path('app_login') }}">je me connecte</a>
    </div>
    <div class="card-body">
        {{ form_start(registration2Form) }}
        <div class="row">
            <div class="col-md-6">
                <div class="form-group mb-3">
                    <div class="form-error">
                        {{ form_errors(registration2Form.lastname) }}
                    </div>
                    {{form_row(registration2Form.lastname, {'label': 'Nom'}) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group mb-3">
                    <div class="form-error">
                        {{ form_errors(registration2Form.firstname) }}
                    </div>
                    {{form_row(registration2Form.firstname, {'label': 'Prénom'}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <div class="form-group mb-3">
                    {{form_row(registration2Form.email, {'label': 'E-mail'}) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group mb-3">
                    <div class="form-error">
                        {{ form_errors(registration2Form.telephone) }}
                    </div>
                    {{form_row(registration2Form.telephone, {'label': 'Téléphone'}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="form-group mb-3">
                    <div class="form-error">
                        {{ form_errors(registration2Form.password) }}
                    </div>
                    {{form_row(registration2Form.password, {'label': 'Mot de passe'}) }}
                </div>
            </div>
        </div>
        <div><small class="form-text text-muted mt-4"><a href="{{ path('page_single', {'slug': 'mentions-legales'}) }}" title="Condition d'utilisation" target="_blank">Lire les conditions générales d'utilisation</a></small></div>
            {{ form_row(registration2Form.agreeTerms, {
                label: 'Accepter les CGU'
            }) }}
        <button class="btn btn-primary">{{ button_label|default('S\'enregistrer') }}</button>
        {{ form_end(registration2Form) }}
    </div>
</div>